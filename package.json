{
  "name": "naive-admin",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/team-naive/naive-admin.git",
  "author": "LSPD",
  "license": "MIT",
  "scripts": {
    "test": "lerna run test",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s -r 0",
    "version": "npm run changelog && git add CHANGELOG.md"
  },
  "devDependencies": {
    "lerna": "^3.14.1"
  },
  "command": {
    "publish": {
      "message": "chore(release): publish",
      "registry": "https://registry.npmjs.org/"
    }
  }
}
